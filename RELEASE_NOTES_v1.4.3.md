# Release Notes v1.4.3

## Key Changes
* **Added `it's` -> `its` possessive rule**: Added the missing inverse homophone rule so that when `it's` (contraction) is incorrectly used in possessive context (such as *"it's bloody sword"*), it is highlighted and corrected to `its`.
