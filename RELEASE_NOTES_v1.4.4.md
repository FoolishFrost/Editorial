# Release Notes v1.4.4

## Key Changes
* **Multi-Colored Spellcheck Heatmap**: Enhanced the spelling checker mode density band rendering. The sidebar heatmap now paints segmented rows containing both **Red** blocks (for misspelled words) and **Blue** blocks (for word confusions), matching the horizontal proportions of each issue type in that section of the text.
